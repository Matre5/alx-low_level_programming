#include "main.h"

/**
 * _abs - finds tge absolute value of a number 
 * @n: the number to br computed 
 *
 *Return: The absolute value of the number
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else 
		return (-n);
}
