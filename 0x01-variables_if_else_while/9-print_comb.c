#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int r;

	r = 48;
	while (r < 58)
	{
		putchar(a);
		if  (r != 57 )
		{
			putchar(44);
			putchar(32);
			r++;
		}
		else 
		{
			r++;
		}
	}
	putchar('\n');
	return (0);
}
